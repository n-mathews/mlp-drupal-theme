{#
/**
 * @file page--user-profile.html.twig
 * Meadow Lane Park — member profile page wrapper.
 * Applied to /user/{uid} via the page__user_profile suggestion added in
 * meadow_lane_theme_suggestions_page_alter() when on entity.user.canonical.
 */
#}
<div class="site-wrapper">

  {{ include('@meadow_lane/partials/site-header.html.twig') }}

  <div class="page-banner">
    <div class="page-banner__inner container">
      <span class="section-label" style="color:rgba(255,255,255,0.55);">
        {{ 'Meadow Lane Park'|t }}
      </span>
      <h1 class="page-banner__title">{{ 'Member Portal'|t }}</h1>
    </div>
  </div>

  {% if page.highlighted %}
    <div class="layout-highlighted container">{{ page.highlighted }}</div>
  {% endif %}

  <div class="member-layout">

    {# ── Sidebar ───────────────────────────────────────── #}
    <aside class="member-sidebar">
      <input type="checkbox" id="member-nav-checkbox" class="member-nav-checkbox" aria-hidden="true">
      <label for="member-nav-checkbox" class="member-nav-toggle">
        <span>{{ 'Member Menu'|t }}</span>
        <span class="member-nav-toggle__icon" aria-hidden="true">
          <span></span><span></span><span></span>
        </span>
      </label>
      <div class="member-nav-collapse" id="member-nav-collapse">
        <nav class="member-nav" aria-label="{{ 'Member navigation'|t }}">
          <ul class="member-nav__list">

            <li class="member-nav__item">
              <a href="/member" class="member-nav__link">
                <svg viewBox="0 0 20 20" fill="none" width="16" height="16" aria-hidden="true">
                  <rect x="2" y="2" width="7" height="7" rx="1" stroke="currentColor" stroke-width="1.5"/>
                  <rect x="11" y="2" width="7" height="7" rx="1" stroke="currentColor" stroke-width="1.5"/>
                  <rect x="2" y="11" width="7" height="7" rx="1" stroke="currentColor" stroke-width="1.5"/>
                  <rect x="11" y="11" width="7" height="7" rx="1" stroke="currentColor" stroke-width="1.5"/>
                </svg>
                {{ 'Dashboard'|t }}
              </a>
            </li>

            <li class="member-nav__item">
              <a href="/announcements" class="member-nav__link">
                <svg viewBox="0 0 20 20" fill="none" width="16" height="16" aria-hidden="true">
                  <path d="M15 7A5 5 0 005 7c0 5.83-2.5 7.5-2.5 7.5h15S15 12.83 15 7z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                  <path d="M11.44 17.5a2 2 0 01-2.88 0" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
                {{ 'Announcements'|t }}
              </a>
            </li>

            <li class="member-nav__item">
              <a href="/events" class="member-nav__link">
                <svg viewBox="0 0 20 20" fill="none" width="16" height="16" aria-hidden="true">
                  <rect x="2" y="3" width="16" height="15" rx="2" stroke="currentColor" stroke-width="1.5"/>
                  <path d="M2 8h16M7 1v4M13 1v4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
                {{ 'Events'|t }}
              </a>
            </li>

            <li class="member-nav__item">
              <a href="/member/documents" class="member-nav__link">
                <svg viewBox="0 0 20 20" fill="none" width="16" height="16" aria-hidden="true">
                  <path d="M11 2H5a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V8l-6-6z" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/>
                  <path d="M11 2v6h6M13 11H7M13 14H7M9 8H7" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
                </svg>
                {{ 'Documents'|t }}
              </a>
            </li>

            <li class="member-nav__item">
              <a href="/history" class="member-nav__link">
                <svg viewBox="0 0 20 20" fill="none" width="16" height="16" aria-hidden="true">
                  <path d="M10 2a8 8 0 100 16A8 8 0 0010 2z" stroke="currentColor" stroke-width="1.5"/>
                  <path d="M10 6v4l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                {{ 'History'|t }}
              </a>
            </li>

          </ul>
        </nav>

        {% if logged_in %}
          <div class="member-sidebar__user">
            <a href="/user/{{ current_uid }}" class="member-sidebar__account is-active">{{ 'My Profile'|t }}</a>
            <a href="{{ logout_url }}" class="member-sidebar__logout">{{ 'Log out'|t }}</a>
          </div>
        {% endif %}
      </div>
    </aside>

    {# ── Main content ──────────────────────────────────── #}
    <main id="main-content" class="member-main" role="main" tabindex="-1">
      {{ page.content }}
    </main>

  </div>

  {{ include('@meadow_lane/partials/site-footer.html.twig') }}

</div>
